---
spring:
  application:
    name: leader-data
  profiles:
    active: dev
  data:
    rest:
      basePath: /api/v1
    neo4j:
      uri: bolt://localhost:7687
      embedded:
        enabled: true
      repositories:
        enabled: true
      username: neo4j
      password: neo4spring_cloud

server:
  port: 8002

---
spring:
  main:
    allow-bean-definition-overriding: true
